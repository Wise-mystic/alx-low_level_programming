#include <stdio.h>
/**
 * main - Point of entry
 * Return: Always return 0 (success)
 */

int main(void)
{
	char c;

	char d;

	d = 'A';
        c = 'a';
	while
		(d <= 'z') {
			putchar(c);
			c++;
		}
	while
		(c <= 'Z') {
			putchar(d);
			d++;
		}
	putchar('\n');
	return (0);
}
