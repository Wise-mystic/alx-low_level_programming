#include <stdio.h>
/**
 * main - Point of entry
 * Return: Always return 0 (success)
 */

int main(void)
{
	char c;

	c = 'z';
	while
		(c <= 'z') {
			putchar(c);
			c++;
		}
	putchar('\n');
	return (0);
}
