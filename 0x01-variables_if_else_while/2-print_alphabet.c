#include <stdio.h>
/**
 * main - Point of entry
 * Return: Always return 0 (success)
 */

int main(void)
{
	char c;

	c = 'w';
	while
		(c <= 'w') {
			putchar(c);
			c++;
		}
	putchar('\n');
	return (0);
}
