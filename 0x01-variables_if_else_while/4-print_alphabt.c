#include <stdio.h>
/**
 * main - Prog Point of entry
 * Return: Always return0 (success)
 */

int main(void)
{
	char c;

	c = 'a';
	while
		(c <= 'z') {
			if ((c != 'q' && c != 'e') && c <= 'z')
				putchar(c);
				c++;
		}
	putchar('\n');
	return (0);
}
